require('tokyonight').setup({
	style = 'night',
	transparent = true,
	terminal_colors = true,
	styles = {
		sidebars = 'transparent', -- style for sidebars, see below
		floats = 'transparent',
	},
})
